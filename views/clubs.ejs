<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Your Clubs</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f4f4;
    }

    .container {
      width: 90%;
      max-width: 1000px;
      margin: 0 auto;
      padding-top: 20px;
    }

    .page-title {
      text-align: center;
      margin-bottom: 20px;
    }

    .card-list {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 20px;
    }

    .card {
      width: 300px;
      border: 1px solid #ccc;
      border-radius: 10px;
      padding: 16px;
      background: white;
      box-shadow: 2px 4px 10px rgba(0, 0, 0, 0.1);
    }

    .card img {
      width: 100%;
      border-radius: 8px;
      height: 180px;
      object-fit: cover;
    }

    .card h2 {
      margin: 12px 0 8px;
      font-size: 1.4rem;
    }

    .card p {
      font-size: 0.95rem;
      color: #333;
    }

    .card button {
      margin-top: 12px;
      padding: 8px 12px;
      background: #007bff;
      color: white;
      border: none;
      border-radius: 6px;
      cursor: pointer;
    }

    .card button:hover {
      background: #0056b3;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="page-title">Clubs Managed by You</h1>

    <% if (clubs.length === 0) { %>
      <p>No clubs found for your PIC ID.</p>
    <% } else { %>
      <div class="card-list">
        <% clubs.forEach(club => { %>
          <div class="card">
            <h2><%= club.name %></h2>
            <p><%= club.email %> | <%= club.type %> </p>
            <p><%= club.funds_utilised %>/<%= club.funds_alloted %> </p>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>
</body>
</html>
